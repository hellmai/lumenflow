# {{VENDOR_NAME}} LumenFlow Rules

**Read first:** [AGENTS.md]({{AGENTS_PATH}}) for universal startup rules, then [LUMENFLOW.md]({{LUMENFLOW_PATH}}) for the canonical workflow, commands, and safety rules.

This file contains {{VENDOR_NAME}}-specific overrides only. Do not duplicate workflow rules from LUMENFLOW.md here.

---

## {{VENDOR_NAME}}-Specific Notes

- Use `pnpm lumenflow:commands` to discover all CLI commands
- Run `<command> --help` before first use of any command
