id: WU-1947
title: '[WU-D] CLI install fix: integrity auto-resolve + real onboard install'
lane: 'Framework: CLI Orchestration'
type: feature
status: ready
priority: P0
created: 2026-02-20
code_paths:
  - packages/@lumenflow/cli/src/pack-install.ts
  - packages/@lumenflow/cli/src/onboard.ts
  - packages/@lumenflow/cli/src/__tests__/pack-install-registry.test.ts
  - packages/@lumenflow/cli/__tests__/onboard-e2e.test.ts
tests:
  manual:
    - Run onboard --yes and verify workspace pack pin includes non-empty integrity
  unit:
    - packages/@lumenflow/cli/src/__tests__/pack-install-registry.test.ts
    - packages/@lumenflow/cli/__tests__/onboard-e2e.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1947.done
dependencies: []
initiative: INIT-032
phase: 2
blocked_by:
  - WU-1945
spec_refs:
  - /home/tom/.claude/plans/curious-skipping-robin.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Merge the pack:install integrity auto-resolve fix with onboard real-install behavior so onboarding no longer writes placeholder pins and registry installs work without manual integrity input.
acceptance:
  - pack:install --id X --source registry --version Y succeeds without manual --integrity
  - onboard --yes installs and pins a concrete version+integrity instead of writing empty placeholder values
  - Registry URL validation enforces HTTPS except localhost, and onboarding handles transient registry failure gracefully
