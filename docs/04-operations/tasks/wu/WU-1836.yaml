id: WU-1836
title: Build pack registry API with Vercel Edge Functions
lane: 'Content: Site Comms'
type: feature
status: ready
priority: P1
created: 2026-02-18
code_paths:
  - apps/web/src/
tests:
  manual:
    - Publish a pack, search for it, verify metadata returned correctly
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1836.done
dependencies: []
initiative: INIT-031
phase: 4
spec_refs:
  - .claude/plans/zazzy-growing-cook.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: api
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Simple registry service: GET /api/registry/packs (list with search), GET /api/registry/packs/:id (metadata, versions, integrity hashes), POST /api/registry/packs/:id/versions (publish new version, authenticated). Pack tarballs stored on Vercel Blob or R2. Registry index is a JSON file updated on publish. Auth via GitHub OAuth.'
acceptance:
  - GET /api/registry/packs returns pack list with search
  - GET /api/registry/packs/:id returns pack metadata and versions
  - POST publish endpoint stores tarball and updates index
  - GitHub OAuth authentication for publishers
