id: WU-2390
title: Suppress git-adapter DEBUG logs by default in wu:claim
lane: 'Framework: CLI WU Commands'
type: bug
status: ready
priority: P2
created: 2026-03-11
code_paths:
  - packages/@lumenflow/cli/src/wu-claim.ts
tests:
  manual:
    - Run wu:claim and verify no DEBUG lines in output without --verbose
  unit:
    - packages/@lumenflow/cli/src/__tests__/wu-claim.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2390.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "wu:claim produces 40+ '[git-adapter] DEBUG: GitAdapter constructor' log lines during a single claim operation. These debug logs clutter agent output and waste context tokens. DEBUG logs should be suppressed unless an explicit --verbose or LOG_LEVEL=debug flag is set."
acceptance:
  - git-adapter DEBUG logs are not shown during normal wu:claim execution
  - DEBUG logs are still available via --verbose flag or LOG_LEVEL=debug
