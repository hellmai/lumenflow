id: WU-2308
title: Fix normalizeSteps silent drop of string steps in routine:create
lane: 'Framework: Core Lifecycle'
type: bug
status: ready
priority: P2
created: 2026-03-03
code_paths:
  - packages/@lumenflow/packs/sidekick/**
tests:
  manual:
    - '(auto) Manual check: verify acceptance criteria; add automated tests before changing code files.'
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2308.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: api
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: normalizeSteps() silently drops string entries, producing misleading INVALID_INPUT error. Should coerce strings to {tool, input:{}} or fail with indexed validation errors.
acceptance:
  - 'String step entries are coerced to {tool: str, input: {}}'
  - Invalid entries produce indexed validation errors instead of silent drop
  - Tests cover all-string, mixed, and empty-after-normalization cases
