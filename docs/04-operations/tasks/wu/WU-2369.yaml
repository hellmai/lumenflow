id: WU-2369
title: Add --replace-test-paths flags to wu:edit
lane: 'Framework: CLI WU Commands'
type: feature
status: ready
priority: P2
created: 2026-03-10
code_paths:
  - packages/@lumenflow/cli/src/wu-edit.ts
tests:
  manual:
    - Run wu:edit --replace-test-paths-unit with new paths and verify old paths are replaced in WU YAML
  unit:
    - packages/@lumenflow/cli/src/__tests__/wu-edit.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2369.done
dependencies: []
plan: lumenflow://plans/WU-2369-plan.md
spec_refs:
  - lumenflow://plans/WU-2369-plan.md
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: wu:edit supports --replace-* flags for code-paths, notes, acceptance, risks, blocked-by, and dependencies, but not for test-paths-unit, test-paths-e2e, or test-paths-manual. Add --replace-test-paths-unit, --replace-test-paths-e2e, and --replace-test-paths-manual flags for parity.
acceptance:
  - wu:edit --replace-test-paths-unit replaces existing unit test paths instead of appending
  - wu:edit --replace-test-paths-e2e replaces existing e2e test paths instead of appending
  - wu:edit --replace-test-paths-manual replaces existing manual test descriptions instead of appending
  - CLI --help output documents the new flags
