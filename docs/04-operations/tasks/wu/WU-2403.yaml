id: WU-2403
title: Fix flaky timeout failures in @lumenflow/initiatives test suite
lane: 'Framework: Initiatives'
type: bug
status: ready
priority: P3
created: 2026-03-12
code_paths:
  - packages/@lumenflow/initiatives/src/__tests__
tests:
  manual:
    - Run pnpm test 3 times consecutively with no failures
  unit:
    - packages/@lumenflow/initiatives/src/__tests__
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2403.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
sizing_estimate:
  estimated_files: 5
  estimated_tool_calls: 25
  strategy: single-session
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: The @lumenflow/initiatives test suite has flaky timeout failures that are pre-existing on main. These failures were surfaced when WU-2402 disabled passWithNoTests:true in vitest.config.ts. The failures are intermittent and relate to async timeout handling in initiatives tests. Needs investigation of which specific tests are timing out and why — likely either too-short timeouts or genuine async race conditions in the test setup.
acceptance:
  - All @lumenflow/initiatives tests pass consistently without timeouts
  - pnpm test passes on main without --skip-gates
