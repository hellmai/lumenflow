id: WU-2383
title: upgrade force-overwrites user docs; init skip leaves LumenFlow content uninjected
lane: 'Framework: CLI WU Commands'
type: bug
status: in_progress
priority: P1
created: 2026-03-10
code_paths:
  - packages/@lumenflow/cli/src/lumenflow-upgrade.ts
  - packages/@lumenflow/cli/src/docs-sync.ts
  - packages/@lumenflow/cli/src/init.ts
  - packages/@lumenflow/cli/src/init-scaffolding.ts
  - packages/@lumenflow/cli/src/merge-block.ts
  - packages/@lumenflow/cli/src/lumenflow-upgrade.ts
  - packages/@lumenflow/cli/src/docs-sync.ts
  - packages/@lumenflow/cli/src/init.ts
  - packages/@lumenflow/cli/src/init-scaffolding.ts
  - packages/@lumenflow/cli/src/merge-block.ts
  - packages/@lumenflow/cli/templates/core/LUMENFLOW.md.template
  - packages/@lumenflow/cli/templates/core/AGENTS.md.template
  - packages/@lumenflow/cli/templates/core/UPGRADING.md.template
  - packages/@lumenflow/cli/templates/core/ai/onboarding/quick-ref-commands.md.template
  - packages/@lumenflow/cli/templates/core/ai/onboarding/starting-prompt.md.template
  - apps/docs/src/content/docs/getting-started/upgrade.mdx
  - apps/docs/src/content/docs/packs/software-delivery/setup/upgrading.mdx
tests:
  manual:
    - Run lumenflow init in project with existing CLAUDE.md — verify user content preserved,
      LumenFlow block injected
    - Run lumenflow:upgrade after user edits outside markers — verify user content preserved
  unit:
    - packages/@lumenflow/cli/src/__tests__/merge-block.test.ts
    - packages/@lumenflow/cli/src/__tests__/docs-sync.test.ts
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2383.done
dependencies: []
risks: []
notes: >-
  SPLIT: WU-2384 (blocked by this WU) covers the narrative doc sweep — Starlight pages, dogfood
  docs, stale docs:sync --force references, ownership table rollout. This WU covers the safety fix +
  scaffold contract + CLI-emitted templates.


  Design (confirmed by Claude + Codex multi-agent review):

  - LUMENFLOW.md: fully managed, force-sync safe. LUMENFLOW.local.md as escape hatch.

  - AGENTS.md + vendor files: thin bootstrap via merge-block markers. Never raw-overwritten.

  - Reserved skills allowlist (exact names, no glob). User skills never touched.

  - Migration guard for first upgrade on repos that hand-edited LUMENFLOW.md.

  - merge-block.ts already implements bounded marker pattern for .gitignore — extend to bootstrap
  surfaces only.

  - Codex constraint: managed block must contain real bootstrap, not just a link. Dumb agents
  (Codex) wont follow references.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: >-
  Safe docs lifecycle: prevent user content destruction on upgrade and enable LumenFlow content
  injection on init.


  ## Design (confirmed by multi-agent analysis)


  ### File ownership model:

  - **LUMENFLOW.md** — fully LumenFlow-managed canonical workflow doc. Force-synced on upgrade.
  Users MUST NOT edit; use LUMENFLOW.local.md for project-specific additions.

  - **AGENTS.md** — thin universal bootstrap with critical operating rules + pointer to
  LUMENFLOW.md. Managed via merge-block markers.

  - **Vendor files** (CLAUDE.md, .cursor/rules/lumenflow.md, .windsurf/rules/lumenflow.md,
  .clinerules) — thin vendor-specific bootstrap + pointer to LUMENFLOW.md. Managed via merge-block
  markers.

  - **LUMENFLOW.local.md** — optional user-owned override file. Never touched by init or upgrade.
  Auto-included by convention.


  ### Upgrade behavior:

  - Force-sync LUMENFLOW.md (it is 100% LumenFlow-managed)

  - Merge-block update bootstrap files (replace content between LUMENFLOW:START/END markers only)

  - Never touch user content outside markers


  ### Init behavior:

  - File missing → create from template (current behavior, unchanged)

  - File exists → inject/update merge-block with bootstrap content, preserve everything else


  ### Skills policy:

  - Reserved LumenFlow skill names (wu-lifecycle, worktree-discipline, lumenflow-gates,
  tdd-workflow) → managed and updatable on upgrade

  - User-created skills outside reserved names → never touched


  ## Bugs fixed:

  1. upgrade calls executeDocsSyncInDir with force:true (lumenflow-upgrade.ts:557) — blitzes
  AGENTS.md, constraints.md, onboarding docs, skills

  2. init skip mode leaves LumenFlow content uninjected when vendor files already exist
acceptance:
  - LUMENFLOW.md is force-synced on upgrade without data loss (it is fully LumenFlow-managed)
  - LUMENFLOW.local.md supported as user override file, referenced in LUMENFLOW.md template header
  - AGENTS.md uses merge-block markers; user content outside markers preserved on upgrade
  - All vendor bootstrap files use merge-block markers; user content preserved on upgrade
  - init into existing project injects merge-block into existing bootstrap files
  - Reserved LumenFlow skills are an exact allowlist; user-created skills never touched
  - 'Migration guard: detect LUMENFLOW.md drift from template before first force-sync; require user
    to move local content to LUMENFLOW.local.md'
  - 'Round-trip test: init -> user edits outside markers -> upgrade -> user edits preserved'
  - 'Codex/dumb-agent test: AGENTS.md managed block contains real minimal bootstrap, not just a link'
  - upgrade syncs only managed surfaces that already exist or are configured — no hardcoded vendor
    assumption
  - LUMENFLOW.md, AGENTS.md, and vendor bootstrap files tell agents to read LUMENFLOW.local.md when
    present
  - 'CLI-emitted templates updated: LUMENFLOW.md.template, AGENTS.md.template,
    UPGRADING.md.template, quick-ref-commands.md.template, starting-prompt.md.template'
claimed_mode: worktree
worktree_path: worktrees/framework-cli-wu-commands-wu-2383
claimed_at: 2026-03-10T20:04:12.189Z
baseline_main_sha: b4881c3c08c3b877c0c0eaab6217f94ef7c8ee08
session_id: a092d60c-086e-471c-b9d5-425495c39d3d
approved_by:
  - tom@hellm.ai
approved_at: 2026-03-10T20:04:12.194Z
