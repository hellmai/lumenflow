id: WU-2399
title: 'Fix and clean up init.ts: --force bug, extract sub-modules, fix gitignore matching'
lane: 'Framework: CLI WU Commands'
type: bug
status: in_progress
priority: P2
created: 2026-03-12
code_paths:
  - packages/@lumenflow/cli/src/init.ts
  - packages/@lumenflow/cli/src/init-templates.ts
  - packages/@lumenflow/cli/src/init-detection.ts
  - packages/@lumenflow/cli/src/init-scaffolding.ts
tests:
  manual:
    - Verify lumenflow init --force updates existing prettier version
    - Verify lumenflow init generates correct gitignore without duplicates
  unit:
    - packages/@lumenflow/cli/src/__tests__/init.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2399.done
dependencies: []
risks: []
notes: Fixed --force flag being a no-op for prettier/scripts, fixed gitignore String.includes()
  false-positive on comments by parsing non-comment lines, extracted 3 sub-modules from init.ts
  (init-docs-scaffolder.ts, init-package-config.ts, init-safety-scripts.ts). All 84 init tests pass
  including 7 new regression tests.
requires_review: false
claimed_mode: worktree
assigned_to: tom@hellm.ai
claimed_at: 2026-03-12T09:51:58.575Z
worktree_path: worktrees/framework-cli-wu-commands-wu-2399
session_id: fd26d1b7-ddbc-450b-b60b-b430cca2f939
exposure: backend-only
sizing_estimate:
  estimated_files: 8
  estimated_tool_calls: 45
  strategy: single-session
escalation_triggers: []
requires_human_escalation: false
approved_by:
  - tom@hellm.ai
approved_at: 2026-03-12T09:51:58.582Z
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Code review found three issues in init.ts: (1) BUG: --force flag is a no-op for
  prettier — inner condition makes force irrelevant at line 1221-1225; (2) scaffoldProject()
  orchestrates 13 sequential operations with mixed concerns — extract init-docs-scaffolder.ts,
  init-package-config.ts, init-safety-scripts.ts; (3) naive gitignore pattern matching uses
  String.includes() which matches comments and partial strings — should parse lines and compare
  trimmed non-comment entries.'
acceptance:
  - --force flag correctly overwrites existing prettier version
  - scaffoldProject() delegates to extracted sub-modules (docs, package config, safety scripts)
  - gitignore deduplication ignores comments and partial matches
  - All existing init tests pass
