id: WU-1755
title: 'Fix new-project friction: flawless lumenflow init to first WU'
lane: 'Framework: CLI WU Commands'
type: feature
status: ready
priority: P2
created: 2026-02-16
code_paths:
  - packages/@lumenflow/cli/src/
  - packages/@lumenflow/core/src/
tests:
  manual:
    - End-to-end golden path test in clean directory
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1755.done
dependencies: []
plan: lumenflow://plans/WU-1755-plan.md
spec_refs:
  - lumenflow://plans/WU-1755-plan.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "Context: 20+ test runs of 'set up lumenflow for my new project' all hit the same friction points. Problem: 10 root causes in init, wu:create, lane:setup, initiative:create, micro-worktree, and wu:status cause a broken golden path. Solution: Fix all 10 friction points (F1-F10) in a single WU across @lumenflow/cli and @lumenflow/core."
acceptance:
  - lumenflow init generates correct paths matching detected docs structure
  - .gitignore template includes .turbo/ and .lumenflow runtime files
  - wu:create --plan satisfies spec-refs for feature WUs
  - initiative:create accepts --description --phase --success-metric
  - lane:setup --lock works in single command
  - wu:status shows worktree status when worktree exists
