id: WU-2391
title: plan:create reports success but silently fails to commit the plan file
lane: 'Framework: CLI WU Commands'
type: bug
status: in_progress
priority: P2
created: 2026-03-11
code_paths:
  - packages/@lumenflow/cli/src/plan-create.ts
tests:
  manual:
    - Run plan:create --from <file> with a simulated git push failure and verify it exits non-zero
      with a descriptive error
  unit:
    - packages/@lumenflow/cli/src/__tests__/plan-create.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2391.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: An agent in lumenflow-cloud ran plan:create --id WU-645 --from <path> which
  reported success. However, no commit was created and no plan file appeared in the repo. git pull
  --ff-only confirmed main was already up to date with no new commits. The micro-worktree appears to
  have been created and cleaned up, but the commit/push step failed silently. Problem: plan:create
  uses micro-worktree isolation to atomically commit plan files. When the internal commit or push
  fails, the command still exits 0 and prints a success message. The agent then proceeded with
  plan:link against the non-existent file, cascading into a chain of workarounds. Solution:
  plan:create must verify the commit landed on the target branch before reporting success. If the
  micro-worktree commit or push fails, the command must exit non-zero and surface the underlying git
  error.'
acceptance:
  - plan:create exits non-zero when micro-worktree commit/push fails
  - plan:create surfaces the underlying git error message on failure
  - plan:create verifies the committed file exists on the target branch before reporting success
claimed_mode: worktree
worktree_path: worktrees/framework-cli-wu-commands-wu-2391
claimed_at: 2026-03-11T11:56:07.309Z
baseline_main_sha: 40e5baffa8e5125676377a5446804396f22b7f18
session_id: 9a5a06bf-f973-4b3c-b7db-f62dcbf12d19
approved_by:
  - tom@hellm.ai
approved_at: 2026-03-11T11:56:07.315Z
