id: WU-1949
title: '[WU-F] Workspace packs route for loaded-pack visibility'
lane: 'Operations: Runtime'
type: feature
status: ready
priority: P1
created: 2026-02-20
code_paths:
  - apps/web/app/api/workspace
  - apps/web/src/server/workspace-connect-handler.ts
  - apps/web/src/components
tests:
  manual:
    - Connect workspace and verify Packs view reflects loaded workspace packs
  unit:
    - apps/web/__tests__/pack-catalog.test.tsx
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1949.done
dependencies: []
initiative: INIT-032
phase: 2
blocked_by:
  - WU-1945
spec_refs:
  - /home/USER/.claude/plans/curious-skipping-robin.md
risks: []
notes: (auto) Add implementation notes, rollout context, or a short summary of the plan/conversation.
requires_review: false
assigned_to: tom@hellm.ai
exposure: api
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Add a dedicated workspace packs API so the dashboard shows packs actually loaded in the connected workspace rather than registry browse results.
acceptance:
  - GET /api/workspace/packs returns currently loaded workspace pack data
  - Packs page consumes workspace-loaded data path and handles empty states clearly
  - Route enforces workspace root validation and rejects invalid path inputs
