id: WU-2418
title: Add DAG branching and routine scheduling for agent-session
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P1
created: 2026-03-12
code_paths:
  - packages/@lumenflow/packs/
  - packages/@lumenflow/runtime/src/
  - packages/@lumenflow/kernel/src/runtime/
tests:
  manual:
    - Run branching and scheduled routine scenarios and verify evidence, wakeups, and finalization remain coherent
  unit:
    - packages/@lumenflow/kernel/src/__tests__/
    - packages/@lumenflow/runtime/src/
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2418.done
dependencies: []
initiative: INIT-52
phase: 4
blocked_by:
  - WU-2414
  - WU-2417
spec_refs:
  - lumenflow://plans/INIT-52-plan.md
risks: []
notes: This WU completes pack-owned orchestration on top of the earlier linear state model.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
sizing_estimate:
  estimated_files: 18
  estimated_tool_calls: 95
  strategy: checkpoint-resume
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Complete the pack-owned orchestration layer with branching, joins, and scheduled routines while keeping scheduled execution inside the existing agent-session task model.
acceptance:
  - The workflow model supports branching and joining
  - Scheduled routines are supported as pack-owned agent-session orchestration rather than a separate execution class
  - Routine execution preserves kernel policy and evidence semantics
  - Scheduled wakeups and workflow finalization are represented coherently in evidence and state transitions
  - Documentation defines what remains kernel-owned versus pack-owned in orchestration
