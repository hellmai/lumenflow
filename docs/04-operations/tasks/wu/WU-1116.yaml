id: WU-1116
title: 'INIT-003 Phase 2: Refactor docs-sync to read from templates'
lane: 'Framework: CLI'
type: refactor
status: ready
priority: P1
created: 2026-01-26
code_paths:
  - packages/@lumenflow/cli/src/docs-sync.ts
  - packages/@lumenflow/cli/src/init.ts
tests:
  manual: []
  unit:
    - packages/@lumenflow/cli/__tests__/docs-sync.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-1116.done
dependencies: []
initiative: INIT-003
phase: 2
blocked_by:
  - WU-1115
labels:
  - refactor
  - templates
  - docs-sync
risks: []
notes: ''
requires_review: false
assigned_to: ''
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: >-
  Remove hardcoded template strings from docs-sync.ts. Instead, read
  templates from bundled files at runtime. This decouples content from code
  and ensures docs-sync always uses the latest templates generated by the
  prebuild sync script.
acceptance:
  - All hardcoded template strings removed from docs-sync.ts
  - Templates read from bundled files using import.meta.url resolution
  - docs-sync command still works correctly after refactor
  - init command updated if needed to use same template loading
  - Template loading is lazy (only when needed, not at module import)
  - Error handling for missing templates with helpful messages
  - Existing tests pass after refactor
