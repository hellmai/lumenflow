id: WU-2401
title: 'Close test coverage gaps: add tests for metrics, memory, runtime, and web packages'
lane: 'Framework: Core Validation'
type: feature
status: ready
priority: P2
created: 2026-03-12
code_paths:
  - packages/@lumenflow/metrics/src
  - packages/@lumenflow/memory/src
  - packages/@lumenflow/runtime/src
  - apps/web/src/server
tests:
  manual:
    - Verify pnpm test passes with no regressions
  unit:
    - packages/@lumenflow/metrics/src/__tests__
    - packages/@lumenflow/memory/src/__tests__
    - packages/@lumenflow/runtime/src/__tests__
    - apps/web/__tests__
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2401.done
dependencies: []
spec_refs:
  - docs/04-operations/_frameworks/lumenflow/wu-sizing-guide.md
risks: []
notes: 'Checkpoint-resume with 3 phases: (1) metrics + runtime tests ~50 calls, (2) memory tests ~50 calls, (3) web tests ~50 calls. Each phase is a natural checkpoint boundary. All changes are additive test files — no production code changes.'
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
sizing_estimate:
  estimated_files: 41
  estimated_tool_calls: 150
  strategy: checkpoint-resume
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Code review found four packages with zero or sparse test coverage: (1) @lumenflow/metrics has 0 test files — DORA/SPACE calculations (deploymentFrequency, leadTime, CFR, MTTR, flowState, bottlenecks) are completely untested; (2) @lumenflow/memory has only 3 test files / 525 lines for 15+ source modules — no tests for checkpoint lifecycle, signal queuing, decay scoring, cleanup; (3) @lumenflow/runtime has 0 test files — Unix socket server, session manager, daemon untested; (4) apps/web has 0 component tests — 60+ React components and server-side input validation (path traversal prevention, CSRF) untested. Additionally, passWithNoTests:true in vitest.config.ts masks these gaps.'
acceptance:
  - metrics package has tests for all DORA/SPACE calculation functions
  - memory package has tests for checkpoint lifecycle, signal queuing, and cleanup
  - runtime package has tests for socket lifecycle and session management
  - apps/web has tests for input-validation.ts server handlers
  - All new tests pass in CI
