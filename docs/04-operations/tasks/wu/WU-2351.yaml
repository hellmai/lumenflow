id: WU-2351
title: Fix no-argument gap in destructive git command deny lists
lane: 'Framework: CLI Enforcement'
type: bug
status: ready
priority: P1
created: 2026-03-09
code_paths:
  - .claude/settings.json
  - packages/@lumenflow/cli/templates/vendors/claude/.claude/settings.json.template
tests:
  manual:
    - '(auto) Manual check: verify acceptance criteria; add automated tests before changing code files.'
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2351.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Claude Code deny patterns use glob syntax (git reset --hard *) which requires trailing args. Bare git reset --hard (no args) slips through. Same gap for git clean -fd. Fix across settings.json, all vendor templates, and lumenflow-cloud.
acceptance:
  - No-argument variants added to .claude/settings.json deny list
  - No-argument variants added to settings.json.template
  - lumenflow-cloud .claude/settings.json patched
