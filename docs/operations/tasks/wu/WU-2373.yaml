id: WU-2373
title: docs:sync should use micro-worktree isolation
lane: 'Framework: CLI WU Commands'
type: bug
status: done
priority: P2
created: 2026-03-10
code_paths:
  - packages/@lumenflow/cli/src/docs-sync.ts
  - packages/@lumenflow/cli/src/lumenflow-upgrade.ts
tests:
  manual:
    - Run docs:sync on main branch and verify it uses micro-worktree instead of writing directly
  unit:
    - packages/@lumenflow/cli/src/__tests__/docs-sync.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2373.done
dependencies: []
risks: []
notes: docs:sync uses micro-worktree isolation on main branch. lumenflow:upgrade integrates
  docs:sync into its transaction. Direct-write preserved in worktrees.
requires_review: false
claimed_mode: worktree
assigned_to: tom@hellm.ai
claimed_at: 2026-03-10T13:36:27.782Z
worktree_path: worktrees/framework-cli-wu-commands-wu-2373
session_id: e48a7038-f06e-4855-a7c7-d8a8350130b8
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
approved_by:
  - tom@hellm.ai
approved_at: 2026-03-10T13:36:27.792Z
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: docs:sync writes files directly to cwd on main branch. The branch guard (WU-1362) only
  warns but doesn't block. Should use micro-worktree isolation like wu:create, wu:claim, and release
  do. Additionally, lumenflow:upgrade should run docs:sync as part of its own micro-worktree
  transaction instead of telling the user to run it separately.
acceptance:
  - docs:sync uses micro-worktree isolation when run on main branch
  - docs:sync commits and pushes atomically like other micro-worktree commands
  - lumenflow:upgrade integrates docs:sync into its own transaction instead of requiring a separate
    manual step
  - Existing worktree behavior preserved (docs:sync in a worktree writes directly as before)
locked: true
completed_at: 2026-03-10T13:44:35.434Z
completed: 2026-03-10
