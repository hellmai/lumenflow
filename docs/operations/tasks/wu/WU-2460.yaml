id: WU-2460
title: Make wu:done auto-rebase tolerate wu:brief evidence-only state drift
lane: 'Framework: CLI WU Commands'
type: bug
status: ready
priority: P2
created: 2026-03-13
code_paths:
  - packages/@lumenflow/cli/src/wu-done.ts
  - packages/@lumenflow/cli/src/wu-done-policies.ts
  - packages/@lumenflow/cli/src/__tests__/wu-done.test.ts
  - packages/@lumenflow/cli/src/__tests__/wu-done-evidence-path.test.ts
tests:
  manual:
    - Run wu:brief for a claimed WU, trigger a parallel-completion auto-rebase path, and verify wu:done completes without manual stashing when only wu-events.jsonl evidence is dirty
  unit:
    - packages/@lumenflow/cli/src/__tests__/wu-done.test.ts
    - packages/@lumenflow/cli/src/__tests__/wu-done-evidence-path.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2460.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: wu:done requires fresh wu:brief evidence for bug/feature WUs, and wu:brief records that evidence in .lumenflow/state/wu-events.jsonl. Problem: when parallel completions trigger wu:done auto-rebase, the rebase path refuses to proceed if the only dirty file is workflow-generated wu-events.jsonl state from a fresh wu:brief run. This creates a completion loop: regenerate required evidence, then fail rebase because the evidence dirtied the worktree. Solution: treat workflow-generated wu:brief/state-only drift as safe during the auto-rebase path, or preserve/absorb it automatically so completion can proceed without manual stash/commit workarounds.'
acceptance:
  - wu:done auto-rebase does not fail when the only dirty worktree change is fresh wu:brief/state evidence in .lumenflow/state/wu-events.jsonl
  - Non-state source edits still block auto-rebase and continue to require explicit user/agent action
  - Regression tests cover the required-evidence plus parallel-completion path so wu:brief and auto-rebase no longer deadlock each other
