id: WU-2426
title: cli-entry.mjs triggers full build on --help flag
lane: 'Framework: CLI WU Commands'
type: bug
status: ready
priority: P2
created: 2026-03-12
code_paths:
  - tools/cli-entry.mjs
tests:
  manual:
    - '(auto) Manual check: verify acceptance criteria; add automated tests before changing code files.'
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2426.done
dependencies: []
risks: []
notes: ''
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: pnpm gates --help runs ensureCliDist which triggers a full pnpm build before gates.js can intercept --help via Commander. Should skip build and use existing dist for help requests.
acceptance:
  - pnpm gates --help shows help without triggering build
  - pnpm gates (no --help) still builds dist if missing
