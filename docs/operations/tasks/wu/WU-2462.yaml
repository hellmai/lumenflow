id: WU-2462
title: Increase CI job timeout from 10 to 20 minutes for ubuntu-latest
lane: 'Operations: Automation'
type: bug
status: ready
priority: P0
created: 2026-03-13
code_paths:
  - .github/workflows/ci.yml
tests:
  manual:
    - Verify CI passes on ubuntu-latest
  unit: []
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2462.done
dependencies: []
risks: []
notes: 'tdd-exception: CI config change - CI itself is the verification'
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: CI job timeout of 10 minutes is insufficient for ubuntu-latest. All individual test timeouts are fixed but the overall job (format+lint+typecheck+test+build+docs) exceeds 10m on shared runners.
acceptance:
  - CI job completes within 20 minute timeout on ubuntu-latest
