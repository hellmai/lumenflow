id: WU-2437
title: 'BUG: gate-co-change crashes with require() in ESM context'
lane: 'Framework: CLI WU Commands'
type: bug
status: ready
priority: P2
created: 2026-03-12
code_paths:
  - packages/@lumenflow/cli/src/gate-co-change.ts
tests:
  manual:
    - Run pnpm gate:co-change --list in consumer project
  unit:
    - packages/@lumenflow/cli/src/__tests__/gate-co-change-cmd.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2437.done
dependencies: []
risks: []
notes: Discovered while debugging config:set failure in lumenflow-cloud. The command is wired in lumenflow:commands but crashes at runtime.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: "gate-co-change.ts line 40 uses require('micromatch') which throws ReferenceError in ESM. The file is .ts compiled to .js in an ESM package (type: module). Fix: convert to import or use createRequire."
acceptance:
  - gate-co-change runs without ESM ReferenceError
  - micromatch imported via ESM import or createRequire
