id: WU-2441
title: Validate config examples against live schemas
lane: 'Content: Framework Docs'
type: feature
status: ready
priority: P2
created: 2026-03-12
code_paths:
  - apps/docs/__tests__/docs-parity.test.ts
  - docs/operations/_frameworks/lumenflow/agent/onboarding/docs-generation.md
  - apps/docs/src/content/docs/getting-started/quickstart.mdx
tests:
  manual:
    - pnpm exec vitest run apps/docs/__tests__/docs-parity.test.ts
    - pnpm --filter @lumenflow/docs build
  unit:
    - apps/docs/__tests__/docs-parity.test.ts
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2441.done
dependencies: []
spec_refs:
  - lumenflow://plans/INIT-52-plan.md
risks: []
notes: Start with the highest-value config surfaces already documented publicly, and keep enforcement tag-gated so narrative snippets are not over-constrained.
requires_review: false
assigned_to: tom@hellm.ai
exposure: documentation
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: 'Context: command and MCP example parity are now enforced, but YAML and config snippets can still drift silently from the live workspace and pack schemas. Problem: public and internal docs may continue to teach invalid config keys, retired shapes, or missing required structure while remaining syntactically plausible. Solution: extend docs parity validation to parse strict configuration examples, validate them against the live config schemas used by the docs/reference generation path, and document how strict versus illustrative config examples should be authored.'
acceptance:
  - Strict configuration examples fail when they use unknown keys, invalid shapes, or stale schema forms against the live config schemas.
  - Illustrative, historical, legacy, or placeholder configuration examples remain supported only when explicitly tagged and documented.
  - Docs authoring guidance explains how strict schema validation applies to YAML or JSON config examples.
