id: WU-2416
title: Add multi-provider adapter support
lane: 'Framework: Core Lifecycle'
type: feature
status: ready
priority: P1
created: 2026-03-12
code_paths:
  - packages/@lumenflow/packs/
  - packages/@lumenflow/kernel/src/
tests:
  manual:
    - Run shared contract fixtures across multiple adapter families and compare normalized behavior
  unit:
    - packages/@lumenflow/kernel/src/__tests__/
    - packages/@lumenflow/runtime/src/
  e2e: []
artifacts:
  - .lumenflow/stamps/WU-2416.done
dependencies: []
initiative: INIT-52
phase: 3
blocked_by:
  - WU-2411
  - WU-2413
spec_refs:
  - lumenflow://plans/INIT-52-plan.md
risks: []
notes: Reuse the conformance harness from the core tool WU instead of inventing separate test shapes here.
requires_review: false
assigned_to: tom@hellm.ai
exposure: backend-only
sizing_estimate:
  estimated_files: 13
  estimated_tool_calls: 70
  strategy: checkpoint-resume
escalation_triggers: []
requires_human_escalation: false
requires_cso_approval: false
requires_cto_approval: false
requires_design_approval: false
description: Support multiple provider adapter families behind stable pack contracts while keeping request and response normalization internal to the pack.
acceptance:
  - The pack supports more than one provider adapter family
  - Adapter selection is config-driven
  - Request and response normalization remains internal to the pack
  - Tests cover provider-specific edge cases and shared contract invariants
